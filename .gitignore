# Código-fonte e pastas de trabalho (mantidas só localmente; não entram no Git)
landing-page/
admin-api/
docs/
_gh-pages-check/

# Cursor IDE
.cursor/

# Questionários / binários locais
*.docx

# OS
Thumbs.db
.DS_Store

# Env / secrets
*.env
.env.*

# CNAME na raiz: no Pages entra via deploy (gh-pages); não versionar no main
/CNAME
